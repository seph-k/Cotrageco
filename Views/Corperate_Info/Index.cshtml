@model IEnumerable<Cotrageco.Models.Corperate_Info>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Corperate_Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Corperate_Image)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Corperate_Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
@*             <td>
                @Html.DisplayFor(modelItem => item.Corperate_Title) 

            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Corperate_Image)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Corperate_Description)
            </td> *@
                <td>
                    @Html.Raw(item.Corperate_Title)
                </td>
                <td>
                    @Html.Raw(item.Corperate_Image)
                </td>
                <td>
                    @Html.Raw(item.Corperate_Description)
                </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Corperate_InfoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Corperate_InfoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Corperate_InfoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
