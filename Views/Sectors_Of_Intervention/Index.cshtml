@model IEnumerable<Cotrageco.Models.Sectors_Of_Intervention>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Sectors_Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sectors_Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sectors_Image)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Sectors_Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sectors_Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sectors_Image)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Sectors_Of_InterventionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Sectors_Of_InterventionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Sectors_Of_InterventionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
