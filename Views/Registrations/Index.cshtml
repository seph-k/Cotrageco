@model IEnumerable<Cotrageco.Models.Registration>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Registration_Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Registration_Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Registration_Image)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
                <td>
                    @Html.Raw(item.Registration_Title)
                </td>
                <td>
                    @Html.Raw(item.Registration_Description)
                </td>
                <td>
                    @Html.Raw(item.Registration_Image)
                </td>

            <td>
                <a asp-action="Edit" asp-route-id="@item.RegistrationId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RegistrationId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RegistrationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
